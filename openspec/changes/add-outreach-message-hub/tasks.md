## 1. Supabase schema 與函式（大腦）

- [x] 1.1 outreach.schedule 新增 send_at timestamptz / source text default 'rule' / tag text / card_override jsonb / alt_text text / recipient_phone text / vars jsonb / dedupe_key text（partial unique index where dedupe_key is not null），並回填既有列 send_at = scheduled_date + interval '1 hour'（UTC 01:00 = 台北 09:00）、source='rule'。行為：排程具備精準發送時間與來源/標籤欄位。驗證：information_schema 八欄齊備、唯一索引存在、既有列 send_at 非空且 source='rule'。（依 design 決策：排程精準到小時：schedule 加 send_at、派送改每小時掃；spec: Time-Precise Hourly Dispatch；spec: Source And Tag Classification On Board）
- [x] 1.2 outreach.rule 的 trigger_basis CHECK 約束擴充允許 'booking_checkout'，插入 key='checkout_d1' 規則列（label 退租前一日提醒、預設卡片含 {{tenant_name}}/{{booking_date}}/{{booking_time}}、enabled=true），並 CREATE OR REPLACE recompute_schedule()：僅處理 trigger_basis in ('contract_start','contract_end') 且插列同時寫 send_at = scheduled_date + interval '1 hour'。行為：退租提醒卡存在且不被合約引擎誤算。驗證：rollback 交易內 recompute → 新列 send_at 均為 01:00 UTC、無任何 rule_key='checkout_d1' 列產生。（依 design 決策：退租提醒：每日傍晚掃 TiDB、發送前再驗（解取消改期）；spec: Checkout Day-Before Reminder）
- [x] 1.3 CREATE OR REPLACE dispatch_due()：挑 status='confirmed' and send_at <= now() 派送（POST 形狀不變）；pg_cron 派送 job 改 0 * * * *（每小時）；新增每日 0 9 * * *（UTC，台北 17:00）job 以 pg_net POST Zeabur /api/outreach/scan-checkout（帶密鑰）。行為：每小時掃、時間到才送；每日傍晚觸發退租掃描。驗證：cron.job 顯示兩筆排程正確；手動 select dispatch_due() 僅撈 send_at 已到的列。（依 design 決策：排程精準到小時：schedule 加 send_at、派送改每小時掃；spec: Time-Precise Hourly Dispatch）

## 2. Zeabur 後端（server/index.ts、server/routers/property/outreachHandlers.ts）

- [x] 2.1 新增 POST /api/outreach/scan-checkout（x-outreach-secret timingSafeEqual）：查 TiDB booking_records join booking_templates（contractAction='退租'、status='confirmed'、bookingTime 落在台北明天 00:00–24:00）→ 逐筆 insert outreach.schedule：source='booking'、rule_key='checkout_d1'、tenant_uid=預約 tenantUid、send_at=台北今天 18:00、status='confirmed'、vars 帶 booking_id/booking_date/booking_time/tenant_name/room、dedupe_key='checkout_d1:'+id+':'+日期，on conflict do nothing；回 { ok, scanned, inserted }。行為：明天退租者今天傍晚進排程且重跑不重複。驗證：造一筆明天退租預約 → 呼叫兩次 → schedule 僅一列、欄位正確（驗收①）。（依 design 決策：退租提醒：每日傍晚掃 TiDB、發送前再驗（解取消改期）；spec: Checkout Day-Before Reminder）
- [x] 2.2 新增 POST /api/outreach/enqueue（x-outreach-secret）：驗 dedupeKey/card/to(uid|phone 擇一)/sendAt/tag 必填（缺→400 指名欄位；密鑰錯→401）；寫 schedule：source='api'、tag、card_override、alt_text、tenant_uid 或 recipient_phone、send_at、status='confirmed'、dedupe_key on conflict do nothing；回 200 { ok, id, deduped }。行為：外部系統可投遞整卡 JSON 進統一排程且可安全重試。驗證：合法投遞出現於看板並可派送；同 dedupeKey 重打回 deduped:true 僅一列；缺欄 400、壞密鑰 401（驗收③）。（依 design 決策：投遞 API：POST /api/outreach/enqueue 收整卡 JSON＋去重碼；spec: External Message Enqueue API）
- [x] 2.3 runOutreach 升級：rule 改 LEFT JOIN、卡片取 coalesce(card_override, rule.card_template)、altText 取 alt_text 或 RULE_ALT_TEXT、computeVars 併入 row.vars；發送前——無 tenant_uid 有 recipient_phone 者查 contract.tenants.primary_phone（正規化去空白/-()）換 line_uid，命中回寫 tenant_uid、查無則 markSendFailure('查無 LINE UID（電話）') 維持可重試不發；source='booking' 列依 vars.booking_id 回查 TiDB 再驗（不存在/非 confirmed/日期非 send_at 隔天 → status='skipped' 寫原因不發）。行為：三來源共用同一發送管線且不誤發。驗證：取消後 sendNow → skipped＋原因（驗收②）；phone 投遞換 UID 成功送達、查無電話留失敗清單（驗收④）。（依 design 決策：發送時電話換 UID（primary_phone 唯一）；依 design 決策：退租提醒：每日傍晚掃 TiDB、發送前再驗（解取消改期）；spec: Phone-To-UID Resolution At Send Time；spec: Checkout Day-Before Reminder）
- [x] 2.4 SCHEDULE_COLS／listSchedule／scheduleSummary 回傳並支援過濾 source、tag、send_at（listSchedule 入參加 source?/tag?）；新增 tRPC listUnboundTenants：查鏡像「有 active 合約（經 tenant_contract_parties）且 line_uid 為空」的主客，回 tenant_name/phone/property_name/room。行為：看板能按來源/標籤查、未綁名單可取得。驗證：listSchedule source='api' 只回 api 列；listUnboundTenants 回傳含四欄位且綁定後消失（驗收⑤⑥前置）。（依 design 決策：看板來源標籤分類與未綁 UID 清單；spec: Source And Tag Classification On Board；spec: Unbound-UID Tenant List）

## 3. 前端（client/src/pages/BookingAdmin/OutreachBoard.tsx、client/src/pages/BookingPublic.tsx）

- [x] 3.1 ScheduleTab 加「來源」篩選鈕（全部/規則/退租提醒/外部API）與「標籤」下拉（distinct tag），列上顯示來源 badge、標籤、send_at 時刻（HH:mm）。行為：一眼分辨並篩選三種來源。驗證：選來源=外部API 僅見 api 列、標籤可選且列上顯示（驗收⑤）。（依 design 決策：看板來源標籤分類與未綁 UID 清單；spec: Source And Tag Classification On Board）
- [x] 3.2 OutreachBoard 新增「未綁UID」分頁：listUnboundTenants 清單（姓名/電話/案場/房號）＋總筆數＋CSV 匯出（UTF-8 BOM，沿用 exportCsv 樣式）。行為：同事可見未綁名單並下載對帳。驗證：分頁有資料、CSV Excel 開啟中文正常（驗收⑥）。（依 design 決策：看板來源標籤分類與未綁 UID 清單；spec: Unbound-UID Tenant List）
- [x] 3.3 規則/卡片頁支援 checkout_d1：RuleEditorCard 與總覽表對 trigger_basis='booking_checkout' 顯示「退租預約」badge（不顯示合約日期字樣），卡片可編輯、可預覽。行為：退租提醒卡與四張卡同地方管理。驗證：規則頁出現 checkout_d1、改文案儲存成功、預覽送達測試 LINE。（依 design 決策：退租提醒：每日傍晚掃 TiDB、發送前再驗（解取消改期）；spec: Checkout Day-Before Reminder）
- [x] 3.4 BookingPublic：verifyByPhone 回 PHONE_NOT_FOUND 時改為提示並 setView('register') 進入既有 RegisterView（姓名 nameInput/電話 phoneInput/入住地址 locationInput/房間編號 roomInput），沿用 registerTenantByPhone 與後續流程。行為：電話查無的主客可自行註冊後繼續預約、有 UID 一併寫回。驗證：輸入查無電話 → 出現註冊畫面 → 送出後回到時段選擇（驗收⑦）。（依 design 決策：電話查無開放自行註冊（接通既有 RegisterView）；spec: Phone-Not-Found Self Registration）

## 4. 部署與端到端驗收（測試模式下）

- [x] 4.1 commit 並 push 觸發 Zeabur 部署，於測試模式（test_redirect_uid 設管理者）下依 design「Implementation Contract」驗收 ①–⑧：明天退租→scan→18:00 排程（重跑去重）；取消後 sendNow→skipped；enqueue 去重與看板可見可送；phone 解析成功與查無留存失敗清單；來源/標籤篩選；未綁UID分頁＋CSV；電話查無→註冊→續流程；recompute 後 send_at=台北 09:00 且 checkout_d1 不被引擎產生。行為：整個發訊中樞在生產環境可用且不誤觸真人。驗證：八項逐一通過並記錄結果。

## 5. 追加項目（提案後補充）

- [x] 5.1 listUnboundTenants 回傳 contract_end_date（active 合約最新 end_date）並支援 expiringWithinDays 參數；「未綁UID」分頁顯示到期日欄位＋「60 天內到期」快速篩選，CSV 匯出含到期日。行為：同事能篩出「該發到期詢問但沒 UID」的名單人工聯繫。驗證：開啟篩選僅列 60 天內到期者且每列顯示到期日；CSV 含該欄。（依 design 決策：看板來源標籤分類與未綁 UID 清單；spec: Expiring Unbound Tenant Visibility）
- [x] 5.2 看板「篩選 / 設定」分頁加「投遞 API 串接範例」可複製卡：端點 URL、x-outreach-secret header、body 欄位說明（dedupeKey / card / to / sendAt / tag）、完整 curl 範例與去重說明。行為：Ragic／MANUS 串接者複製即可打通 enqueue。驗證：照卡上範例發一筆 → 看板出現該排程（搭配驗收③）。（依 design 決策：投遞 API：POST /api/outreach/enqueue 收整卡 JSON＋去重碼；spec: Enqueue Integration Example）
